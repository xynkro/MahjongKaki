# Multiplayer Design (Phase 3)

Placeholder — architecture decisions deferred until Phase 1 and Phase 2 are complete.

Key constraints already established:
- Engine is a pure state machine: `(state, action) → newState`
- All game state serialisable as JSON
- No server dependency for local play
